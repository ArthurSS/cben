#include <stdio.h>
int main()
{
	float f = 2.3333;
	printf("this is a plain float number: %.2f\n", f);
	printf("%s\n", "This is a plain string.");
	printf("Convert 108 to unsigned hexadecimal, using uppercase letters:%X\n", 108);
	//printf("?! %!\n", "! will flush the output?");
	return 0;
}